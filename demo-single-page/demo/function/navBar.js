import { ref } from 'vue'
export const navBarHeight = ref(0)      // 整个导航栏高度（状态栏+标题栏）
export const statusBarHeight = ref(0)   // 状态栏高度
export const titleBarHeight = ref(0)    // 标题栏高度（胶囊按钮以下部分）
export const navBarColor = ref('2c2c2c')
export const navBarBackColor =ref('#fe4543')
export const title_text = ref('')